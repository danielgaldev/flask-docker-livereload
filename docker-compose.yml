version: '3'

services: 
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - 5000:5000
  livereload:
    build:
      context: livereload
    ports:
      - 35729:35729
    volumes:
      - ./src:/livereload
      